[Unit]
Description=test docker compose unit
After=docker.service

[Service]
ExecStart={{ docker_compose_default_path }} -f {{ item.root_dir }}/docker-compose.yml up
ExecStop={{ docker_compose_default_path }} -f {{ item.root_dir }}/docker-compose.yml down

[Install]
WantedBy=docker.service
